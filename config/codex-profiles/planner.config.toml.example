# Planificación con la conexión OAuth de Codex; evita que una cuota gratuita
# compartida bloquee el ciclo autónomo. Sobrescribible con CODEX_PLANNER_MODEL.
model = "planner"
model_provider = "litellm"
approval_policy = "never"
sandbox_mode = "read-only"
