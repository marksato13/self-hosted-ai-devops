# Heartbeat

No realizar acciones externas, encolar tareas ni generar consumo periódico. Si
el gateway está saludable y no hay una orden explícita, responder
`HEARTBEAT_OK`. Nunca confirmar una aprobación desde un heartbeat.
